# Local-dev stack for the Rails API.
#
# Two ways to use this:
#
#   1. Full containerized stack (API + worker + Postgres):
#        cp apps/api/.env.example apps/api/.env   # optional — see notes
#        docker compose up
#      → API on http://localhost:3000, Solid Queue worker, Postgres.
#      Source is bind-mounted, so edits hot-reload (Rails dev reloader).
#      First boot installs gems + runs db:prepare (creates + seeds).
#
#   2. Postgres only (run web/mobile/API natively, as before):
#        docker compose up -d postgres
#        cd apps/api && bin/rails db:prepare && bin/rails s -p 3000
#
# Mobile (Expo) always runs natively — it needs simulator/device access
# that doesn't containerize cleanly. Web can run natively (`pnpm dev`,
# port 3001) against the containerized API on :3000.
#
# Auth: POSTGRES_HOST_AUTH_METHOD=trust matches database.yml defaults
# (user=postgres, password=""). Safe because the port is bound to
# localhost only. Dev use only.

# Shared config for the API + worker — same image, same mounts, same DB.
x-api-base: &api-base
  build:
    context: ./apps/api
    target: dev
  environment:
    DATABASE_HOST: postgres
    RAILS_ENV: development
    # apps/api/.env (loaded below) can carry the PRODUCTION DATABASE_URL, and
    # Rails lets DATABASE_URL override database.yml for the running env — so
    # without this pin, `docker compose up` would boot dev (and db:prepare!)
    # straight at the production database. Host/user only, no database path:
    # database.yml keeps naming biteworthy_dev / biteworthy_test per env, so
    # a test-env command in the container hits the test database. `environment`
    # beats `env_file` for the same key, which is what makes the pin stick.
    # (Native commands are guarded separately by the checked-in
    # apps/api/.env.development / .env.test, which dotenv loads before .env.)
    #
    # Two consequences of any DATABASE_URL being set: boot's db:prepare no
    # longer auto-creates biteworthy_test (Rails skips the test sibling when
    # the var is present — run `docker compose exec -e RAILS_ENV=test api
    # bin/rails db:prepare` once before in-container specs), and a
    # RAILS_ENV=production command in the container now resolves to this
    # local postgres (with no database name) instead of reaching Neon —
    # deliberate: a laptop container should never quietly talk to prod.
    DATABASE_URL: postgresql://postgres@postgres:5432
  env_file:
    # Optional: holds ANTHROPIC_API_KEY etc. The stack boots without it;
    # only the ingestion pipeline needs the key. `cp .env.example .env`.
    - path: ./apps/api/.env
      required: false
  volumes:
    - ./apps/api:/rails
    - bundle:/usr/local/bundle
  depends_on:
    postgres:
      condition: service_healthy
  restart: unless-stopped

services:
  postgres:
    image: postgres:16
    container_name: biteworthy-postgres
    ports:
      - '127.0.0.1:5432:5432'
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # Rails API (Puma). Clears any stale server pid, self-heals gems if the
  # Gemfile changed since the last build, prepares the dev databases
  # (create + load schema + seed on first run), then boots on 0.0.0.0.
  api:
    <<: *api-base
    container_name: biteworthy-api
    command: >
      bash -c "rm -f tmp/pids/server.pid &&
               (bundle check || bundle install) &&
               ./bin/rails db:prepare &&
               ./bin/rails server -b 0.0.0.0 -p 3000"
    ports:
      - '127.0.0.1:3000:3000'
    healthcheck:
      test: ['CMD-SHELL', 'curl -fsS http://localhost:3000/up || exit 1']
      interval: 10s
      timeout: 5s
      retries: 12
      # Generous: covers first-boot bundle install + db:prepare.
      start_period: 90s

  # Solid Queue worker (background jobs — the ingestion pipeline). Waits
  # for the API to be healthy so db:prepare has loaded the queue schema
  # before this starts polling. Same command the Kamal `worker` role runs.
  worker:
    <<: *api-base
    container_name: biteworthy-worker
    command: >
      bash -c "(bundle check || bundle install) &&
               ./bin/rails solid_queue:start"
    depends_on:
      postgres:
        condition: service_healthy
      api:
        condition: service_healthy

volumes:
  pgdata:
  bundle:
