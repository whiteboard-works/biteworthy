/**
 * /admin landing. The ops dashboard (cost buckets, community spend vs
 * ceiling, queue badges) replaces this placeholder in the next PR of
 * the workstream.
 */
export default function AdminHomePage() {
  return (
    <main data-testid="admin-home">
      <h1 className="text-bw-2xl font-bold text-zinc-900">Admin</h1>
      <p className="mt-bw-2 text-bw-sm text-zinc-600">The ops dashboard lands here next.</p>
    </main>
  );
}
