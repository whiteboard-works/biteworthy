User-agent: *
Allow: /

# Phase 5.4 — Next.js proxy routes shouldn't be indexed; they're
# server-side adapters to the Rails API, not user-facing pages.
Disallow: /api/

# The admin backoffice is signed-in, admin-only chrome — never index.
# (Its layout also serves noindex metadata; this saves the crawl.)
Disallow: /admin

Sitemap: https://bite-worthy.com/sitemap.xml
