# ActionCable is unused (API-only app, no channels). The async adapter
# is in-process and needs no backing store; revisit if realtime features
# ever ship (solid_cable + a dedicated cable table was the old intent).
development:
  adapter: async

test:
  adapter: test

production:
  adapter: async
